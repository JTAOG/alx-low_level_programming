#include <stdio.h>

/**
 * main - this is my enrty point
 *
 * Return: (0) is a success
 */
int main(void)
{
	char f;

	for (f = 'a'; f <= 'z'; f++)
	{
		putchar(f);
	}
	putchar('\n');
	return (0);
}

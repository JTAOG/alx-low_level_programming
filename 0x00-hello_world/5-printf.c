#include <stdio.h>

/**
 * main - this is my entry point
 *
 * Return: if this returns (0),it is a success.
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
